import { Box, Container, FormControl, styled } from "@mui/material";

export const StyledGenderContainer = styled(Container)({
  display: "flex",
});

export const StyledAgeBox = styled(Box)({
  width: "5rem",
});

export const StyledFormControl = styled(FormControl)({
  width: "5rem",
});
